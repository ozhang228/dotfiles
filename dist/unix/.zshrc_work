# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/ozhang/miniconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/ozhang/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/home/ozhang/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/ozhang/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

alias ca="conda activate desk-tools"
alias sdr="p3 fio/desk_tools/apps/sdr_viz_server/main.py configs/sdr_viz_server/local.json "

# Used in nvim to open up ai
export AI_CLI_CMD="agent"

# colors in conda
export CONDA_CHANGEPS1=false

alias ssc="maim --select | xclip -selection clipboard -t image/png"
alias dtc="cd typescript/packages/@desk-tools/common/"

export PATH=~/.local/bin:~/.cargo/bin:/usr/local/bin:$PATH
export CDPATH=.:~/src:~/src/opensource:~/src/fixed-income-options:~/src/ficc:~/src/cumberland:~/src/opensource:~/src/$(whoami)
[ -s "$HOME/.scm_breeze/scm_breeze.sh" ] && source "$HOME/.scm_breeze/scm_breeze.sh"
export PNPM_HOME="$HOME/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac


function dev-ficco {
    source ~/.ficcorc
}

function dev-cumberland {
    source ~/.cumberlandrc
}

export JAVA11_HOME=/usr/lib/jvm/java-1.11.0-openjdk-amd64
export JAVA17_HOME=/usr/lib/jvm/java-1.17.0-openjdk-amd64
export JAVA21_HOME=/usr/lib/jvm/java-1.21.0-openjdk-amd64
export JAVA25_HOME=/usr/lib/jvm/java-1.25.0-openjdk-amd64

function jdk11 {
  export JAVA_HOME=$JAVA11_HOME
  export PATH=$JAVA_HOME/bin:$PATH
}

function jdk17 {
  export JAVA_HOME=$JAVA17_HOME
  export PATH=$JAVA_HOME/bin:$PATH
}

function jdk21 {
  export JAVA_HOME=$JAVA21_HOME
  export PATH=$JAVA_HOME/bin:$PATH
}

function jdk25 {
  export JAVA_HOME=$JAVA25_HOME
  export PATH=$JAVA_HOME/bin:$PATH
}
