[metadata]
name = "Polybar config"
supported_distros = ["ubuntu"]

[[deps]]
id = "polybar"
[deps.install_method]
ubuntu = "sudo apt install polybar"

[[deps]]
id = "pavucontrol"
[deps.install_method]
ubuntu = "sudo apt install pavucontrol"

[[deps]]
id = "blueman"
[deps.install_method]
ubuntu = "sudo apt install blueman"
