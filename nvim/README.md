# Dependencies

- `ripgrep`
- `fzf`
- `gh`
- `lazygit`
- `clang-tidy`
- `treesitter-cli` (through cargo)
- any clipboard manager
- A nerd font, preferably JetBrains Mono
- AI_CLI_CMD env var

- Blink cmp
  -lazydev
  -snippets
- Dap
- Obsidian
- Image (see if snakcs image works) / Diagram
- TS-GO lsp
- arrow nvim
- plugin for better picker in monorepos
  - should define as a series of paths in my config, ie create a "monorepo" config file, then have a series of paths and only search within those paths
- LSP saga for better renames
- friendly snippets and come up with good snippets
- shortcuts for common actions
  - turning a const funciton into a real function and vice versa
